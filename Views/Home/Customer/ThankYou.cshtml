@{
    Layout = null;
    ViewData["Title"] = "Cảm ơn";
}

<style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
        }

        .video-bg {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            object-fit: cover;
            z-index: -1;
        }

        .overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 0;
        }

        .content {
            position: relative;
            z-index: 1;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            color: white;
            text-align: center;
            padding: 0 20px;
        }

        h2 {
            font-size: 2.5rem;
            font-weight: bold;
        }

        p {
            font-size: 1.2rem;
            margin-top: 1rem;
        }

        .btn-home {
    margin-top: 2rem;
    padding: 12px 28px;
    font-size: 1rem;
    border-radius: 8px;
    border: 2px solid white;
    background-color: transparent;
    color: white;
    text-decoration: none;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.btn-home:hover {
    background-color: white;
    color: #000;
}

    </style>
<body>

    <!-- Video nền -->
    <video autoplay muted loop class="video-bg">
    <source src="~/video/demo.mp4" type="video/mp4" />
    Trình duyệt của bạn không hỗ trợ video nền.
    </video>


    <!-- Lớp mờ đen phủ video -->
    <div class="overlay"></div>

    <!-- Nội dung chính -->
    <div class="content">
        <h2>Cảm ơn bạn đã tin tưởng và đặt hàng tại Moon 🌛</h2>
        <p>Đơn hàng của bạn đã được chúng tôi tiếp nhận.<br />Chúng tôi sẽ xử lý sớm nhất có thể.</p>
        <p>Vui lòng liên hệ ngay cho chúng tôi nếu bạn có thắc mắc hoặc khiếu nại.</p>
        <a asp-controller="Home" asp-action="Index" class="btn-home">Quay lại trang chủ</a>
        <a asp-controller="Home" asp-action="Index" class="btn-home">Theo dõi đơn hàng</a>
    </div>

</body>

